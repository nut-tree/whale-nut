# CI container image with "headless" VNC session

This repository contains a Docker images with headless VNC environment and OpenCV.

It is used to run CI jobs for [nut.js](https://github.com/nut-tree/nut.js)